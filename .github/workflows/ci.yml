name: CI Demo

on:
  push:
    branches: ["main"]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show files
        run: ls -la

      - name: Run a fake test
        run: |
          echo "Running tests..."
          echo "All tests passed ✅"

